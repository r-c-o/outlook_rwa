# Session exports
chat_session.json
chat_session2.json
*.jsonl

# Local dev artifacts (session logs, scratch notes) — not tracked
.dev/

# Local-only files: kept on disk but not tracked
.claude/
REVIEW.md
__do_git.py
__git_log.txt
__git_tmp.txt

# Machine-specific config override (paths, Q0) — never commit
config/config.local.yaml

# Python
__pycache__/
*.py[cod]
*.pyo
.env
*.egg-info/
build/
dist/

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# Data files (large / potentially sensitive)
data/
*.xlsx
*.parquet
*.duckdb

# Output
output/
parquet_cache/

# VS Code
.vscode/
*.code-workspace

# OS
desktop.ini
.DS_Store
Thumbs.db
